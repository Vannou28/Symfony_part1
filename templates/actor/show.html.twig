{# templates/program/show.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}Actor {{ actor.name}}{% endblock %}

{% block body %}

   <h2>{{ actor.name }}</h2>
    <h3>Listes des series oÙ se trouve ce gars : </h3>
        <ul>
            {% for program in actor.programs %}
                <a href="/programs/show/{{ program.id }}">
                    <li> {{ program.title }} </li>
                </a>
            {% endfor %}
        </ul>
    <a href="{{ path('program_index') }}">
        Retour à la liste des programmes
    </a>
    <strong>Acteurs :</strong>
    
    

{% endblock %}